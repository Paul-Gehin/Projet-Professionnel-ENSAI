---
title: "R Notebook"
output: html_notebook
---

```{r}
library(altair)
```

```{r}
vega_data <- import_vega_data()

data <- vega_data$barley()
```

```{r}
alt$Chart(data)$
  mark_bar()$
  encode(
    y = 'variety',
    x = 'sum(yield)',
    color = 'site'
  )
```
